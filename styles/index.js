import * as styledComponents from 'styled-components';

import { getRgba } from './helpers';

const {
  default: styled,
  css,
  injectGlobal,
  keyframes,
  ThemeProvider,
  ServerStyleSheet,
} = styledComponents;

export {
  css,
  injectGlobal,
  keyframes,
  ThemeProvider,
  ServerStyleSheet,
  getRgba,
};
export default styled;
